export interface Anime {
    id: string,
    title: string,
    author: string,
    producedBy: string,
    genres: [],
    startDate: string,
    endDate: string,
    description: string,
    imageUrl: string,
    currentlyWatched: number,
    completed: number,
    dropped: number,
    ownerId: string
}
